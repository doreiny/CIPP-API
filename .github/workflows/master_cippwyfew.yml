name: Deploy to Azure

on:
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v2

    - name: Deploy to Azure
      uses: azure/functions-action@v1
      with:
        app-name: cippwyfew
        package: '.'
